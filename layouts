<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ page.title }} | {{ site.title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      background: #fcfcf7;
      margin: 0;
      font-family: 'Inter', Arial, Helvetica, sans-serif;
      color: #222;
    }
    .header {
      text-align: center;
      padding: 40px 0 10px 0;
      font-size: 2.5rem;
      font-weight: 700;
      letter-spacing: 2px;
      background: none;
    }
    .main-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      max-width: 700px;
      margin: 0 auto;
      padding: 40px 10px 30px 10px;
      background: none;
    }
    .post-image {
      max-width: 420px;
      width: 100%;
      margin-bottom: 32px;
      border-radius: 12px;
      box-shadow: 0 4px 24px 0 rgba(0,0,0,0.05);
    }
    .post-meta {
      text-align: center;
      margin-bottom: 18px;
      font-size: 1rem;
      color: #444;
    }
    .meta-label {
      background: #111;
      color: #fff;
      padding: 2px 8px;
      font-size: 0.9rem;
      font-weight: 700;
      border-radius: 3px;
      margin-right: 8px;
      letter-spacing: 0.5px;
      display: inline-block;
    }
    .post-title {
      text-align: center;
      font-size: 2.2rem;
      font-weight: 900;
      margin-bottom: 12px;
      margin-top: 0;
      line-height: 1.13;
    }
    .post-author {
      text-align: center;
      font-size: 1rem;
      margin-top: 8px;
      color: #222;
    }
    .footer {
      text-align: center;
      padding: 32px 8px 18px 8px;
      font-size: 0.97rem;
      color: #888;
      border-top: 1px solid #eee;
      margin-top: 60px;
      letter-spacing: 0.5px;
    }
    @media (max-width: 600px) {
      .main-content { padding: 20px 2vw 20px 2vw; }
      .header { font-size: 1.5rem; }
      .post-title { font-size: 1.3rem; }
      .post-image { max-width: 98vw; }
    }
  </style>
</head>
<body>
  <div class="header">
    {{ site.title }}
  </div>
  <div class="main-content">
    {{ content }}
  </div>
  <div class="footer">
    &copy; {{ "now" | date: "%Y" }} {{ site.title }} &mdash; All rights reserved.
  </div>
</body>
</html>
